# Setup

1. Open `KenKaneki23-ux/KenKaneki23-ux` on GitHub.
2. Replace the repository's `README.md` with this `README.md`.
3. Create `assets/` and upload `assets/matrix.svg`.
4. Commit both files to `main`.
5. Refresh the profile.

The LinkedIn badge currently points to the LinkedIn homepage; replace that URL with the user's actual profile URL before publishing.
